# Repository Working Agreement

이 파일은 이 저장소에서 작업하는 Codex와 다른 자동화 에이전트가 따라야 할 프로젝트 규칙이다.

## 작업 시작 전에 읽을 문서

다음 순서로 현재 상태를 확인한다.

1. `README.md`
2. `PROJECT_GUIDE.md`
3. `docs/TECHNICAL_REVIEW.md`
4. `docs/ROADMAP.md`
5. `docs/WORKLOG.md`의 최신 기록

문서 간 상태가 다르면 `docs/ROADMAP.md`를 작업 상태의 단일 기준으로 사용한다. 사실과 검증 결과는 가장 최근의 `docs/WORKLOG.md` 기록을 우선한다.

## 기본 작업 절차

1. 코드 변경 전 관련 Roadmap 항목과 완료 조건을 확인한다.
2. 작업을 시작할 때 해당 항목을 `진행 중`으로 바꾸고 `현재 작업` 섹션에 ID를 기록한다.
3. 한 번에 하나의 Roadmap 항목만 `진행 중`으로 둔다.
4. 요청 범위 밖의 문제를 발견하면 즉시 함께 고치지 말고 Roadmap 후보로 기록한다.
5. 코드 변경은 작고 검증 가능한 단위로 수행한다.
6. 실행, 빌드 또는 테스트 결과를 성공과 실패 모두 `docs/WORKLOG.md`에 기록한다.
7. 완료 조건을 모두 충족한 경우에만 Roadmap 항목을 `완료`로 바꾼다.
8. 사용자 확인이나 정확한 Unity 버전이 필요하면 상태를 `보류`로 두고 이유와 재개 조건을 기록한다.

단순 조사나 문서 검토만 수행하는 경우 Roadmap 상태를 바꿀 필요는 없지만, 새로운 사실이 기존 기준선을 바꾸면 관련 문서를 갱신한다.

## 문서별 갱신 규칙

- `README.md`: 외부 방문자가 알아야 할 기능, 실행법, 검증 상태 또는 대표 결과가 달라질 때 갱신한다.
- `PROJECT_GUIDE.md`: 프로젝트의 장기 목적, 범위 또는 완료 기준이 바뀔 때만 갱신한다.
- `docs/TECHNICAL_REVIEW.md`: 중요한 위험이 해소되거나 구조 평가가 달라질 때 갱신한다. 일상적인 진행 상태는 기록하지 않는다.
- `docs/ROADMAP.md`: 우선순위, 상태, 완료 조건과 다음 작업을 관리한다.
- `docs/WORKLOG.md`: 날짜별 변경 이유, 수정 파일, 검증 명령·결과, 남은 문제와 결정을 누적한다. 과거 기록을 결과에 맞춰 다시 쓰지 않는다.

## 구현 원칙

- Unity 프로젝트 요구 버전은 `6000.0.34f1`이다. 다른 버전으로 프로젝트를 자동 업그레이드하지 않는다.
- 기존 사용자 변경사항과 씬·프리팹 직렬화 변경을 임의로 되돌리지 않는다.
- 콘텐츠 추가보다 실행 복구, 명백한 버그, 데이터화와 성능 측정을 우선한다.
- 대규모 싱글톤/매니저 재작성은 별도 Roadmap 항목과 사용자 합의 없이 진행하지 않는다.
- C#은 기존 스타일을 존중하되 새 필드는 가능하면 `[SerializeField] private` 또는 `private`로 제한한다.
- `Update`/`FixedUpdate`의 반복 검색, 할당과 전체 컬렉션 순회를 추가하지 않는다.
- 이벤트 구독과 코루틴은 대응되는 해제·중단 수명주기를 명확히 한다.
- 오브젝트 이름과 `Resources` 문자열 경로에 대한 새 의존을 추가하지 않는다.
- 외부 서버 주소, 인증 정보와 외부 에셋은 공개 리스크를 먼저 확인한다.

## 검증과 보고

- 빌드하지 못한 상태를 빌드 성공으로 표현하지 않는다.
- Unity 버전이나 외부 서버 때문에 검증하지 못했다면 환경 제약과 필요한 재개 조건을 기록한다.
- 가능하면 수정 전 재현 방법과 수정 후 검증 방법을 함께 남긴다.
- 자동화 테스트가 없는 기능은 수동 스모크 테스트 항목을 구체적으로 작성한다.
- Git 커밋, 브랜치 생성과 원격 작업은 사용자가 요청했을 때만 수행한다.
