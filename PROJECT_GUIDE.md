# 2D Survival Project Guide

## 프로젝트 역할

이 프로젝트는 포트폴리오의 **보조 기술 프로젝트**로 사용한다. 완성된 생존형 게임 루프를 기반으로 다음 두 가지를 증명하는 것이 핵심 목적이다.

1. Excel 원본, 검증 컨버터와 Unity용 생성물을 이용한 데이터 기반 설계
2. 다수 개체 환경에서 오브젝트 풀과 프로파일링을 이용한 성능 개선

새로운 적이나 무기의 수를 늘리는 것보다 기존 구조를 측정 가능하고 설명 가능한 형태로 개선하는 것을 우선한다.

## 보존할 강점

- 전투, 성장, 웨이브, 결과 화면으로 이어지는 완결된 게임 루프
- 다수 개체가 등장하는 장르에 오브젝트 풀을 적용한 판단
- `IDamageable`, `IDroppable`, `IMagnetic`로 표현한 행동 경계
- 서로 다른 공격 방식을 보여주는 탄막, 검, 곡괭이, 오오라, 폭발 무기
- UnityWebRequest를 이용한 로그인·회원가입·랭킹 연동 경험

## 개선 원칙

- 기능 추가 전에 현재 동작을 재현하고 기록한다.
- 명백한 버그와 수명주기 문제를 작은 단위로 먼저 수정한다.
- Excel을 원본 데이터로 사용하고 C# 컨버터에서 검증한 뒤 Unity용 데이터를 생성한다.
- 대규모 매니저 재작성보다 특전 수직 구현, 드롭, 적·무기, 웨이브 순서로 점진적으로 데이터화한다.
- 생성 결과는 직접 수정하지 않고 동일 입력에서 동일 결과가 나오도록 관리한다.
- 최적화는 추측이 아니라 동일 환경의 Profiler 측정 전후로 설명한다.
- 사용하지 않는 외부 에셋과 데모 리소스는 공개 범위에서 제외한다.
- 과거 구현의 한계를 숨기지 않고 당시 선택과 현재 개선 이유를 함께 기록한다.

## 범위 제한

- 데이터화와 성능 검증 전에 적·무기 콘텐츠를 대량으로 추가하지 않는다.
- 전체 싱글톤/매니저 구조를 한 번에 교체하지 않는다.
- 게임 규칙, 저장 데이터와 입력 시스템의 대규모 변경은 별도 계획 없이 진행하지 않는다.
- 외부 서버가 필요한 기능과 로컬 게임 루프를 가능한 한 분리한다.
- 데이터 파이프라인이 안정화되기 전에는 별도 저장소나 Git 서브모듈을 필수 구조로 만들지 않는다.

## 장기 완료 기준

- 특전, 드롭, 적·무기 능력치와 웨이브를 Excel에서 수정하고 자동 검증·변환할 수 있다.
- 잘못된 ID, 타입, 범위와 데이터 참조가 Unity 실행 전에 차단된다.
- 메인 → 게임 → 결과 흐름에 대한 재현 가능한 스모크 테스트 기록이 있다.
- 100·300·500개체 조건의 CPU, GC, Physics2D와 프레임 시간 결과가 있다.
- README에서 풀링 전략, 데이터 모델과 측정된 최적화 결과를 설명한다.
- 외부 에셋의 출처와 라이선스가 정리되어 공개 범위를 판단할 수 있다.

## 문서 운영

- 최초 기술 평가와 위험: `docs/TECHNICAL_REVIEW.md`
- 현재 우선순위와 진행 상태: `docs/ROADMAP.md`
- 날짜별 작업과 검증 결과: `docs/WORKLOG.md`

구체적인 작업 상태는 이 문서가 아니라 `docs/ROADMAP.md`를 단일 기준으로 사용한다.

## 결정 기록

- 2026-06-23: 신규 콘텐츠보다 데이터화와 성능 최적화를 보여주는 보조 프로젝트로 지정했다.
- 2026-07-17: 저장소 문서 체계를 기준선, 로드맵, 작업 기록으로 분리하고 진행 상태는 Roadmap에서만 관리하기로 했다.
- 2026-07-17: 데이터화는 Excel 원본과 C# 컨버터를 중심으로 진행하고, 별도 데이터 저장소·서브모듈은 파이프라인 안정화 후 재평가하기로 했다.
